import React from "react";
import "../assets/styles/components/propiedad.css";

const TipoPropiedad = ({ tipoPropiedades }) => {
  debugger;
  return (
    <div>
      <p>{tipoPropiedades.nombre}</p>
    </div>
  );
};

export default TipoPropiedad;
